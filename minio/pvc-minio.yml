kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: minio-pv-claim
spec:
  storageClassName: nfs-client #manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 190Gi
