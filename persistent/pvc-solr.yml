kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: task-solr-pv-claim
spec:
  storageClassName: nfs-client #manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 190Gi
